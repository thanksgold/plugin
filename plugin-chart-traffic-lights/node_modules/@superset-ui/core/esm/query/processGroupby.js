export default function processGroupby(groupby) {
  const groupbyList = [];
  groupby.forEach(value => {
    if (typeof value === 'string') {
      groupbyList.push(value);
    }
  });
  return groupbyList;
}