export default class ColorScheme {
  constructor(config) {
    const {
      colors,
      description = '',
      id,
      label
    } = config;
    this.id = id;
    this.label = label != null ? label : id;
    this.colors = colors;
    this.description = description;
  }

}